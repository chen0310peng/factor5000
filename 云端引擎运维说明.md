# 云端引擎运维说明（live_engine）

> 2026-09-15 上线。波段/日内引擎 24h 运行在 GitHub Actions，每 15 分钟一轮；
> 网页 `BTC_ETH实时策略信号.html` 顶部"☁️ 云端引擎"面板只读展示。**波段/日内账本以云端为准**。

---

## 1. 架构一句话

```
GitHub Actions（每15min）
  └─ live_engine.py
       ├─ 拉币安(5镜像)/OKX行情 + 仓库里的 selected_top500.json（每日08:17回测更新）
       ├─ 动态因子层实时求值 → 波段S / 日内z 信号
       ├─ 仓位管理（开仓/补仓/三档止盈/止损/反转平仓/追单保护/防御闸门）
       └─ 状态写回 data/live_state.json → 网页面板读取展示
```

- 超短线（5m 周期）**不在云端**，仍由浏览器端运行——Actions 频率跟不上 5m。
- 浏览器里的本地账本保留作对照，不再作为波段/日内的执行依据。

## 2. 首次部署

```bash
git add live_engine.py .github/workflows/live_engine.yml
git commit -m "live engine: 24h cloud swing/intraday engine"
git push
```

然后到 GitHub → Actions → **live engine (15min)** → Run workflow 手动触发一次。
首轮会生成 `data/live_state.json` 并自动提交回仓库。

## 3. 日常怎么看

| 看什么 | 在哪里 |
|---|---|
| 当前持仓/最近交易/事件 | 网页顶部"☁️ 云端引擎"面板（每 5 分钟自动刷新） |
| 原始状态 | 仓库 `data/live_state.json`（持仓 positions / 日内 positions_intra / 交易 trades / 事件 events） |
| 运行日志 | Actions → live engine (15min) → 点进某次运行 → "run live engine" 步骤 |
| 交易邮件 | 开仓/平仓/止损等交易类事件发生时自动发件（复用 MAIL_* secrets） |

日志里每轮会打印：双币现价与数据源、波段 S 与动态层分值、日内信号、闸门开关、本轮全部事件。
看到 `⚠️ …异常` 不用慌——单模块异常被隔离，其余模块照常运行，下一轮自动恢复。

## 4. 规则参数怎么调

全部集中在 `live_engine.py` 顶部配置区，改完 push 即生效（下一轮起）：

| 参数 | 默认 | 含义 |
|---|---|---|
| `SW_TRAIL_ARM_ATR` | 0.5 | 波段浮盈达到 0.5×ATR 激活回撤保护 |
| `SW_TRAIL_GIVEBACK` | 0.5 | 浮盈从峰值回吐 50% → 平仓落袋 |
| `SW_TIME_STOP_DAYS` | 5 | ≥5 天未达止盈1 → 止损上移至成本 |
| `SW_MAX_HOLD_DAYS` | 15 | ≥15 天 → 市价强平 |

不想用某条规则：把对应值调大即可（如 `SW_MAX_HOLD_DAYS = 9999` 等于关闭超时强平）。

## 5. 常见运维操作

**暂停云端开仓（但不平仓）**：在 `live_engine.py` 的 `run()` 里给两个 `trade_engine*` 调用前加 `continue`，push；
或直接到 Actions 页面 Disable workflow（全部暂停，持仓脱管，**不推荐**——止损也没人看了）。

**手动平掉一笔云端持仓**：编辑仓库 `data/live_state.json`：
把 `positions`（波段）或 `positions_intra`（日内）里对应币种的值改为 `null`，并在 `trades` 里把
对应 `exitPrice: null` 的记录补上 `status:"已平仓"、exitPrice、exitTime、reason:"人工干预平仓"`，提交即可。
下一轮引擎会按新状态继续。

**触发一轮立即运行**：Actions → live engine (15min) → Run workflow。
（行情剧烈波动时不用等 15 分钟整点。）

**改巡检频率**：编辑 `.github/workflows/live_engine.yml` 的 `cron: '*/15 * * * *'`
（最低建议 5 分钟；免费版 cron 有数分钟抖动，对日 K 波段/4h 日内无影响）。

## 6. 故障排查

| 现象 | 排查 |
|---|---|
| 面板"更新于"时间停在很久以前 | Actions 页面看最近运行是否全红；常见原因是币安镜像全挂（日志会写"币安5镜像全部不可用→OKX兜底"），OKX 也挂才整轮跳过 |
| 连续多轮无信号、无持仓 | 正常——阈值内不开仓；日志里看 S 值与闸门状态确认 |
| 有信号但不开仓 | 日志会写明原因：追单保护（已走出>1×ATR）/ 防御冷却期 / 消息面冻结 / 日内闸门（近7日模拟为负）/ 凯利上限≈0 |
| 邮件不发了 | 检查仓库 Settings → Secrets 里 MAIL_USER/MAIL_PASS/MAIL_TO 是否还在；QQ 邮箱授权码过期需重设 |
| 因子池日期不更新 | 看 daily factor backtest 工作流（每天 08:17）是否成功，与 live engine 相互独立 |

## 7. 与每日因子回测的关系

- `daily factor backtest`（每天 08:17）：重算全库 → 更新 `data/factor5000/selected_top500.json`（谁上岗）
- `live engine (15min)`（每 15 分钟）：用当天的上岗池算实时信号并管理仓位（怎么交易）
- 两者独立运行，互不妨碍；因子池更新后下一轮 live engine 自动生效。
