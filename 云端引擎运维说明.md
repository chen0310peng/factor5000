# 云端引擎运维说明（live_engine）v2

> 2026-09-15 上线，09-20 更新。波段 / 日内 / 超短线三套引擎全部 24h 运行在 GitHub Actions，
> **每 5 分钟一轮**；网页 `BTC_ETH实时策略信号.html` 顶部"☁️ 云端引擎"面板只读展示。
> **所有账本以云端为准**，本地浏览器引擎已全部关闭，只镜像显示。

---

## 1. 架构一句话

```
GitHub Actions（每 5 分钟）
  └─ live_engine.py
       ├─ 拉币安(5镜像)/OKX行情 + 仓库里的 selected_top500.json（每天 08:17 回测更新）
       ├─ 动态因子层实时求值 → 波段S / 日内z / 超短线ssig 信号
       ├─ 仓位管理（开仓/补仓/三档止盈/止损/反转平仓/追单保护/防御闸门/消息面冻结）
       └─ 状态写回 data/live_state.json → 推回仓库 → 网页面板每 5 分钟读取展示
```

另有两个独立工作流：`morning report`（每天 08:05 发晨报邮件）、
`daily factor backtest`（每天 08:17 更新因子上岗池）。三者互不干扰。

## 2. 三套引擎的开平仓规则（都不是等比区间，全部锚定 ATR 波动率）

| | 波段（日K） | 日内（15m） | 超短线（5m，09-18 上云） |
|---|---|---|---|
| 信号 | 动态池综合分 S，阈值 ±1.2 左右 | 日内 z 分 + 闸门 | 动态池 ssig（静态 6 均值回复因子兜底），阈值 ±1.2，高波动 ±1.5 |
| 第一手 | 凯利仓位 | 凯利仓位 | 固定 10%（高波动减半） |
| 补仓 | 浮亏 1×ATR 补一次 | 同左 | 浮亏 1×ATR5 补 7% |
| 止损 | 1.5~2×ATR（动态） | 同左 | 1.5×ATR5 |
| 止盈 | 三档 +1/+2/+3×ATR | 三档 | 三档 +1/+2/+3×ATR5，止盈1后止损移成本锁盈 |
| 时间规则 | ≥5天未达止盈1→止损移成本；≥15天→强平 | 当日了结 | 限时 60 分钟到点平仓；连续 2 次止损→暂停开新仓 60 分钟 |
| 其他保护 | 浮盈≥0.5×ATR 激活回撤保护，回吐 50% 落袋；反转平仓 | 同波段思路 | 4h 单边市禁止逆势单；追单保护（走出 0.6×ATR5 不追，等回踩） |

> 关键：ATR 是实时波动率，行情波动大止盈止损就自动放远，波动小就收紧——
> 不是固定的等比网格。消息面密集时的错单主要来自静态区间不适应波动突变，
> 现在的 ATR 锚定 + 防御层 + 120 分钟冻结就是为这个设计的。

## 3. 日常怎么看

| 看什么 | 在哪里 |
|---|---|
| 当前持仓/最近交易/事件 | 网页顶部"☁️ 云端引擎"面板（每 5 分钟自动刷新，含超短线） |
| 原始状态 | 仓库 `data/live_state.json`（positions / positions_intra / positions_scalp / trades / events） |
| 运行日志 | Actions → live engine (5min) → 点进某次运行 → "run live engine" 步骤 |
| 交易邮件 | 开仓/补仓/止盈/止损等事件触发时自动发 HTML 卡片邮件（含入场价、补仓位、止损位、三档止盈位） |
| 晨报 | 每天 08:05（因子池状态 + 持仓 + 当日宏观事件） |

日志里每轮打印：双币现价与数据源、各模式信号分值、闸门开关、本轮全部事件。
单模块异常会被隔离，其余模块照常运行，下一轮自动恢复。

## 4. 规则参数怎么调

全部集中在 `live_engine.py` 顶部配置区，改完 push 即生效（下一轮起）：

| 参数 | 默认 | 含义 |
|---|---|---|
| `SW_TRAIL_ARM_ATR` | 0.5 | 波段浮盈达到 0.5×ATR 激活回撤保护 |
| `SW_TRAIL_GIVEBACK` | 0.5 | 浮盈从峰值回吐 50% → 平仓落袋 |
| `SW_TIME_STOP_DAYS` | 5 | ≥5 天未达止盈1 → 止损上移至成本 |
| `SW_MAX_HOLD_DAYS` | 15 | ≥15 天 → 市价强平（杜绝僵尸单） |
| 消息面冻结 | ≥120 分钟 | 防御警报触发后冻结开新仓，因子自定义更长时从其长（09-19 由 60 提至 120） |

## 5. 常见运维操作

**暂停云端开仓（但不平仓）**：在 `live_engine.py` 的 `run()` 里给各 `trade_engine*` 调用前加 `continue`，push；
或到 Actions 页面 Disable workflow（全部暂停，持仓脱管，**不推荐**——止损也没人看了）。

**手动平掉一笔云端持仓**：编辑仓库 `data/live_state.json`，把 `positions` / `positions_intra` / `positions_scalp`
里对应币种改为 `null`，并在 `trades` 里把对应 `exitPrice: null` 的记录补上
`status:"已平仓"、exitPrice、exitTime、reason:"人工干预平仓"`，提交即可，下一轮按新状态继续。

**触发一轮立即运行**：Actions → live engine (5min) → Run workflow。

**改巡检频率**：编辑 `.github/workflows/live_engine.yml` 的 `cron: '*/5 * * * *'`
（最低建议 5 分钟；免费版 cron 有数分钟抖动，属正常）。

## 6. 故障排查

| 现象 | 排查 |
|---|---|
| 收到 GitHub "Run failed" 邮件 | 单次失败先看后续轮次是否已恢复——瞬时网络抖动会自愈；连续失败才要查日志 |
| 面板"更新于"停在很久以前 | Actions 看最近运行是否全红；常见是币安镜像全挂（日志写"切 OKX 兜底"），OKX 也挂才整轮跳过 |
| 连续多轮无信号、无持仓 | 正常——阈值内不开仓；日志看分值与闸门状态确认 |
| 有信号但不开仓 | 日志会写明原因：追单保护 / 防御冷却期 / 消息面冻结 / 闸门（近7日模拟为负）/ 凯利上限≈0 |
| 邮件不发了 | 检查仓库 Settings → Secrets 的 MAIL_USER/MAIL_PASS/MAIL_TO；QQ 邮箱授权码过期需重设 |
| 因子池日期不更新 | 看 daily factor backtest 工作流（每天 08:17）是否成功，与 live engine 相互独立 |

### 已修复案例：2026-09-20 单次 Run failed
两轮 5 分钟运行的存档提交撞车，`git pull --rebase` 冲突 → 整轮标红（仅该轮状态没存上，下一轮自愈）。
已给 live_engine / daily_factor 两个工作流的推送步骤加了**冲突自愈**：
失败自动重试 3 次，冲突时以本轮最新状态为准重放提交。同类故障不会再出现。

## 7. 与每日因子回测的关系

- `daily factor backtest`（每天 08:17）：重算全库 → 更新 `data/factor5000/selected_top500.json`（谁上岗）
- `live engine (5min)`（每 5 分钟）：用当天的上岗池算实时信号并管理仓位（怎么交易）
- 两者独立运行，互不妨碍；因子池更新后下一轮 live engine 自动生效。
